# API Reference

Complete reference for the public API of `{{projectName}}`.

## Installation

```sh
npm install {{projectName}}
```

## Exports

<!-- Document each exported symbol. Example format below. -->

### `functionName(param: Type): ReturnType`

<!-- Description of what it does. -->

**Parameters**

| Name    | Type   | Description        |
| ------- | ------ | ------------------ |
| `param` | `Type` | What this param is |

**Returns**

`ReturnType` — description of the return value.

**Example**

```ts
import { functionName } from '{{projectName}}';

const result = functionName(value);
```

---

<!-- Repeat the pattern above for each exported symbol. -->
